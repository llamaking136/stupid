#include <iostream>
#include <sstream>
#include <string>
#include <fstream>
#include <stdlib.h>

using namespace std;

namespace header {

    void compileFile(string path) {
        cout << path << endl;
    }

    void runFile(string path) {
        // ree
    }

}